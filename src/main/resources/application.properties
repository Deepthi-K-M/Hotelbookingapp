# Application Name
spring.application.name=hotelbooking

# 1. DATABASE CONNECTION (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=deepthi1909
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. JPA / HIBERNATE SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# 3. H2 CONSOLE
spring.h2.console.enabled=true

# 4. FILE UPLOAD SETTINGS
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

# 5. LOGGING
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# 6. MAIL
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sundori.57ch.64ig@gmail.com
spring.mail.password=sgugawsngdqhyvmv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 7. APP NAME
app.name=Luxury Stay

# 8. SESSION TIMEOUT FIX
server.servlet.session.timeout=60m